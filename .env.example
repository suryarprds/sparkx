# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Choose your database connection:
# Option 1: Local SQLite (development)
# Option 2: Azure PostgreSQL (remote/production)

# ACTIVE DATABASE - Change to switch between local and remote
# Set to "local" for SQLite or "remote" for Azure PostgreSQL
DB_ENV="remote"

# Local SQLite Database
LOCAL_DATABASE_URL="file:./prisma/dev.db"

# Remote Azure PostgreSQL Database
# Replace YOUR_PASSWORD with your actual database password
# For production, set this in Azure App Service Configuration (not in code)
REMOTE_DATABASE_URL="postgresql://sparkxadmin:YOUR_PASSWORD@sparkx.postgres.database.azure.com:5432/postgres?sslmode=require"

# Production: Set DATABASE_URL in Azure App Service
# Azure Portal > App Service > Configuration > Application settings
# Add: DATABASE_URL = postgresql://sparkxadmin:PASSWORD@sparkx.postgres.database.azure.com:5432/postgres?sslmode=require

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
PORT=3001
NODE_ENV=development

# CORS Settings
CORS_ORIGIN="http://localhost:8080"

# API Configuration
API_BASE_URL="http://localhost:3001/api"

# ============================================================================
# PRODUCTION CONFIGURATION (Azure App Service)
# ============================================================================
# Set these in Azure Portal > App Service > Configuration > Application Settings
# PORT=8080 (usually auto-set by Azure)
# NODE_ENV=production
# CORS_ORIGIN=https://sparkx.azurewebsites.net
# AZURE_APP_URL=https://sparkx.azurewebsites.net
# DATABASE_URL=postgresql://user:pass@server.postgres.database.azure.com:5432/db?sslmode=require
